# nacos配置
server:
  port: 80

spring:
  application:
    name: nacos-payment-consumer
  cloud:
    nacos:
      discovery:
        server-addr: tongxmwj.fun:8848 #Nacos服务注册中心地址
      config:
        server-addr: tongxmwj.fun:8848 #Nacos作为配置中心地址

  # ${spring.application.name}-${spring.profile.active}.${spring.cloud.nacos.config.file-extension}






